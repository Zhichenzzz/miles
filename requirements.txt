accelerate
blobfile
datasets
httpx[http2]
mcp[cli]
memray  # needed for debugging (but is lightweight), we can put it to dev mode when using pyproject.toml
omegaconf
peft>=0.13.0  # for LoRA training
pillow
pybase64
pylatexenc
pyyaml
qwen_vl_utils # for VLM
ray[default]
ring_flash_attn
sglang-router>=0.2.3
tensorboard
transformers
wandb
